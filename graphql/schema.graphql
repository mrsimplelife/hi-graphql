type Movie {
  id: Int!
  name: String!
  score: Int!
}

type Query {
  readMovies: [Movie]!
  readMovie(id: Int!): Movie
}

type Mutation {
  createMovie(name: String!, score: Int!): Movie!
  deleteMovie(id: Int!): Boolean!
}
